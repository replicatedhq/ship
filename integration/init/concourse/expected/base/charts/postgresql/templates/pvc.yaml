apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: postgresql
    release: concourse
  name: concourse-postgresql
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
