apiVersion: v1
kind: Service
metadata:
  labels:
    app: concourse-worker
    release: concourse
  name: concourse-worker
spec:
  clusterIP: None
  ports: []
  selector:
    app: concourse-worker
  type: ClusterIP
