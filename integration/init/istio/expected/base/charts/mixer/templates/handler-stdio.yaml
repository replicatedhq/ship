apiVersion: config.istio.io/v1alpha2
kind: handler
metadata:
  labels:
    app: mixer
    chart: mixer
    heritage: Tiller
    release: istio
  name: stdio
  namespace: default
spec:
  compiledAdapter: stdio
  params:
    outputAsJson: true
