apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: jenkins
    release: jenkins
  name: jenkins
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
