apiVersion: v1
kind: Service
metadata:
  name: {{ template "stable-mysql.fullname" . }}
  labels:
    app: {{ template "stable-mysql.name" . }}
    chart: {{ template "stable-mysql.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "stable-mysql.name" . }}
    release: {{ .Release.Name }}
